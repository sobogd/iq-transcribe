generator client {
  provider      = "prisma-client-js"
  binaryTargets = ["native", "linux-arm64-openssl-3.0.x"]
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Transcription {
  id         String   @id @default(cuid())
  ownerKey   String? // "telegram:<id>" | "device:<uuid>"
  title      String   @default("")
  fileName   String
  audioUrl   String? // S3 url of the extracted audio (mp3) — kept for playback + retry
  mimeType   String   @default("")
  sizeBytes  Int? // original file size in bytes
  language   String   @default("") // detected spoken language, e.g. "ru" | "es" | "en"
  transcript String   @default("")
  summary    String   @default("")
  status     String   @default("done") // "processing" | "done" | "error"
  error      String? // failure reason when status = "error"
  createdAt  DateTime @default(now())

  @@index([ownerKey])
  @@map("transcriptions")
}
